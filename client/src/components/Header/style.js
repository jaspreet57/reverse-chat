import styled from 'styled-components';
import { Jumbotron } from 'react-bootstrap';

const HeaderContent = styled(Jumbotron)`
  padding: 5px;
  h1 {
      text-align: center;
  }
`;

export default HeaderContent;
